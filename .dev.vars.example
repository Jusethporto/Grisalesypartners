# Copy this file to .dev.vars (gitignored) for local `wrangler dev` testing.
# In production, this is set as a Secret in the Cloudflare dashboard instead:
# Workers & Pages -> grisalesypartners -> Settings -> Variables and secrets.
CONTACT_TO_EMAIL=your-real-inbox@example.com
